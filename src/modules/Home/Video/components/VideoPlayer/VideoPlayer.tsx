import { Box } from "@mui/material";

const VideoPlayer = () => {
  return (
    <Box
      sx={{
        aspectRatio: "8/4",
        background: "gray",
        borderRadius: "12px"
      }}
    ></Box>
  );
};

export default VideoPlayer;
